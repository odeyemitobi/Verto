{
  "name": "verto",
  "version": "1.0.0",
  "description": "Privacy-first Bitcoin invoicing platform with trustless escrow",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:report": "vitest run -- --coverage --costs",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@hirosystems/clarinet-sdk": "^2.0.0",
    "@stacks/transactions": "^6.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
